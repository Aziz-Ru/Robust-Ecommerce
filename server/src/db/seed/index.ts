import categoryFactory from "./categoryFactory";

const main = async () => {
  // await productFactory(1000);
  // await productImageFactory(5000);
  // await categoryFactory(5);
  process.exit(0);
};

main();
